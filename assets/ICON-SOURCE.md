The Pokeball artwork was supplied by the user from:
https://toppng.com/free-image/pokeball-PNG-free-PNG-Images_26938

The source page labels the artwork "Free for personal use".
`pokeball-source.png` is the supplied original.
`pokeball-filled.png` is the edited version used for the app icon, produced with
the built-in image generation tool. Edit prompt: fill the lower interior and
central button with opaque white, preserving the red area, dark outlines,
diagonal orientation, and transparent exterior.
Run
`powershell -ExecutionPolicy Bypass -File scripts/build-icon.ps1`
to regenerate `tokemon.png` (512 px) and `tokemon.ico` (16–256 px).
